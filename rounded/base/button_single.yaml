button_single:
  variables:
    color: blue
  aspect_ratio: 1/1
  styles:
    card:
      - border-radius: 24px
      - background-color: "[[[ return 'var(--' + variables.color + ')'; ]]]"
    icon:
      - color: var(--black)