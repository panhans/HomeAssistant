button_single:
  variables:
    color: blue
  aspect_ratio: 1/1
  styles:
    card:
      - border-radius: 30%
      - background-color: "[[[ return 'var(--' + variables.color + ')'; ]]]"
    icon:
      - color: var(--black)